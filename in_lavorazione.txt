Bug trovati:
Da CD, un driver ogni tanto muore appena avviato (raro, non chiuso).
    Sintomo: «page fault a 0x00000000, EIP=0x00000000», e nel dump lo STACK
    del processo e' 0x0..0x0 mentre heap e VMA sono quelli di un altro
    programma. Visto tre volte su una cinquantina di avvii da CD, sempre sul
    primo comando di /boot/avvio.sh, piu' facilmente con poca memoria (16-20
    MB) e mai con loglevel=4 — cioe' un problema di TEMPI, non di RAM.
    Il 24 agosto 2026 e' stata chiusa la strada che lo spiega: fra
    proc_create() e proc_set_ready() il processo stava in PROC_BLOCKED con un
    PID gia' vivo, e chiunque svegliasse un bloccato PER PID (ipc_send, il
    VFS, i pty) lo faceva partire con EIP=0 e senza stack. Adesso quello
    stato e' PROC_NASCENTE e nessuna via di risveglio lo conosce; in piu'
    sched_unblock_locked scrive un [WARN] se qualcuno ci prova.
    NON E' PROVATO CHE FOSSE QUELLA: 18 avvii senza la correzione non l'hanno
    riprodotto, quindi la misura non distingue. Se il fault ricompare, il
    [WARN] dira' se e' quella strada o un'altra.


Da fare, per una pagina web moderna (in ordine di quanto pesa):

1. GLI SCRIPT DI COLLEGAMENTO CJK, e cioe' i font. Cinese, giapponese,
   coreano, devanagari e thai escono come collegamenti VUOTI — sottolineati e
   senza lettere. Non e' il lettore TrueType: ne' Liberation ne' DejaVu hanno
   quei glifi, e chi li ha (Noto CJK) pesa venti megabyte su un CD che ne pesa
   dieci. Le strade sono due, e nessuna e' gratis: un font CJK ridotto ai soli
   caratteri comuni, oppure dichiararlo a schermo invece che lasciare un
   collegamento vuoto che sembra rotto.

2. @media, le unita' relative (em, %, rem) e JavaScript: dichiarati fuori.
   Le unita' relative sono le piu' vicine a essere fattibili — `font-size`
   accetta solo i pixel — ma vogliono la misura del contenitore, che
   l'impaginazione a penna corrente non ha in mano quando serve.

3. Le GIF animate mostrano il primo fotogramma, ed e' dichiarato: animarle
   vorrebbe dire un orologio dentro la libreria delle immagini.

4. FATTO — I BORDI DELLE TABELLE. `<table border="1">` adesso si legge e
   disegna un filo intorno a ogni cella e intorno alla tabella. E' l'attributo,
   non il CSS: e' come si sono disegnate le tabelle per vent'anni, e le pagine
   che lo usano sono le stesse che non hanno un foglio di stile. I bordi stanno
   nello stesso elenco degli sfondi — sono rettangoli sotto il testo e sopra lo
   sfondo, esattamente come uno sfondo — cosi' non c'e' un secondo tetto da
   scegliere ne' un secondo ritaglio da sbagliare. `border-collapse`, i colori e
   i lati separati non ci sono.

5. L'ARABO E L'EBRAICO SI VEDONO MA VANNO NEL VERSO SBAGLIATO. I glifi ci
   sono (DejaVu), ma si impaginano da sinistra a destra come tutto il resto:
   una riga di arabo si legge al contrario. Il BiDi non c'e' ed e' un lavoro
   suo, non un ritocco.

6. IL CURSORE NON SI PIAZZA COL MOUSE. Nelle caselle e nelle aree si muove
   con le frecce, Home, Fine e le cancellazioni; un clic dentro il testo pero'
   da' il fuoco alla casella e mette il cursore in fondo, non nel punto in cui
   si e' cliccato.


FATTI IL 25 AGOSTO 2026 (chiesti nella stessa giornata):

A. FATTO — install lancia hwconfig in fondo. Il kernel.cfg appena copiato e'
   quello del supporto d'installazione: elenca i driver che servivano a LUI,
   non alla macchina su cui si e' appena installato. Il primo avvio da disco e'
   proprio il momento in cui un driver mancante si paga, e li' non c'e' piu'
   modo di lanciare hwconfig per ripararlo.
   ! GLI SI PASSA LA RADICE DEL DISCO, non niente: senza argomento hwconfig
   riscrive il kernel.cfg del sistema che sta GIRANDO — quello del CD — e il
   disco resta con l'elenco sbagliato, cioe' il caso che si voleva evitare.
   ! E SI CHIEDE, non si fa: hwconfig riscrive il file per intero, ed e' una
   decisione. Chi dice di no se lo sente dire come rimediare.

B. FATTO — la lingua, chiesta all'installazione e scritta in [kernel] di
   kernel.cfg. Il kernel NON la usa per niente: tradurre e' lavoro dei
   programmi, e ognuno sa quali messaggi ha. Il kernel la conserva e la
   riconsegna con getenv("lingua"), esattamente come fa con `keymap` — cosi'
   non ci sono due elenchi di lingue che divergono.
   ! E hwconfig NON LA CANCELLA: sta fra le voci che si riportano com'erano,
   insieme a `login` e `svga`.
   ! IL DIFETTO TROVATO PROVANDO: la voce si scriveva solo sulla FUSIONE, cioe'
   reinstallando sopra. Sull'installazione pulita — il caso normale —
   kernel.cfg si copia e basta, quindi si chiedeva la lingua e non la si
   scriveva da nessuna parte. Adesso anche il file appena copiato passa di li'.
   Provato: scelto «francais», e nel kernel.cfg del disco c'e' `lingua = fr`.

C. FATTO — exwin sulla console 5, ci si va e si torna.
   Le console erano quattro e la grafica se ne prendeva una: chi lavorava in
   testo ne aveva tre senza averlo chiesto. Adesso sono cinque, Alt+F1..F4
   restano di chi scrive. VGA_N_CONSOLE e KBD_N_CONSOLE devono combaciare, ed
   e' scritto in tutt'e due i posti.
   Digitando `exwin` si passa alla grafica SUBITO, e quando la grafica si
   spegne si torna da soli alla console di partenza.
   ! LA CONSOLE DELLA GRAFICA ESISTE SOLO MENTRE LA GRAFICA C'E'. Finche' il
   server non gira, Alt+F5 non fa niente: li' ci sarebbe uno schermo nero con
   una shell che nessuno guarda, e chi ci finisce non sa come tornare. Da
   DENTRO la grafica, Alt+F1..F4 funzionano come sempre e Alt+F5 riporta alla
   scrivania. E se la grafica gira altrove (`exwin -c 2`) l'ultima console
   torna a essere una console di testo come le altre.
   Il meccanismo e' SYS_CONSOLE_GRAFICA (kernel 0.207): lo stato sta nel
   KERNEL e non nel server, cosi' un server ucciso libera la console da solo.

D. FATTO — `exit` dal program manager spegne la scrivania.
   Prima chiudeva solo il pm e lasciava la grafica accesa senza nessuno
   dentro: per spegnerla davvero bisognava sapere cosa uccidere. Adesso il
   server manda a OGNI applicazione la stessa chiusura della crocetta — un
   messaggio che sanno gia' gestire, quindi chi ha da salvare fa in tempo —
   aspetta al massimo due secondi, rimette il modo testo e muore. Le
   applicazioni non si uccidono: gli si CHIEDE.
   Provato dal CD: pm chiede, wserver spegne, exwin riporta alla console di
   partenza, e lo schermo torna 720x400 cioe' modo testo.


E. FATTO — la risoluzione, nelle impostazioni della scrivania.

   ! E IL MECCANISMO C'ERA GIA' TUTTO. Nella nota precedente avevo scritto il
   contrario — «/bin/svga non esiste e Stage 2 sceglie da se'» — e mi ero
   sbagliato leggendo solo il ciclo che scorre i modi VESA. Sopra quel ciclo
   c'e' il cancello: `svgamodo`, un byte dentro Stage 2 marcato dalla firma
   'SVGAMODE', con la tabella 1=640x480 2=800x600 3=1024x768 e 0=testo. Se
   quel byte e' zero si resta in testo; se e' valido, la risoluzione voluta
   finisce in vwant/hwant e il ciclo cerca QUEL modo.
   E il programma che lo scrive c'e': e' `/dev/svga.drv`, non /bin/svga — sta
   fra i driver perche' cosi' entra nel catalogo ed e' `hwconfig -d` a
   installarlo. Scrive due cose che devono restare d'accordo: la voce `svga`
   in kernel.cfg (la configurazione) e il byte dentro Stage 2 (il recapito per
   chi si avvia prima che esista un filesystem).

   Mancava solo l'interfaccia, e adesso c'e': Avvio > Impostazioni... apre una
   finestra che mostra la risoluzione di ADESSO (chiesta al server, non
   indovinata) e i quattro modi. Premendone uno si lancia /dev/svga.drv e si
   dice com'e' andata.
   ! IL RIAVVIO SI DICE, perche' il modo video lo sceglie l'avvio: quando la
   scrivania gira, quella porta e' chiusa da un pezzo.
   ! E IL FALLIMENTO NON SI INDOVINA. Da CD non si puo' scrivere — LOADER.BIN
   e' in sola lettura — ed e' il caso piu' comune: dire «la scheda non offre
   quel modo» manderebbe a cercare un difetto dove non c'e'. Si dicono i due
   casi veri e si rimanda alla console, dove svga stampa il motivo esatto.


DA FARE (in coda):

F. Taglia/copia/incolla: C'E' GIA' ed e' di tutta la scrivania —
   `ex_area_copia`, `ex_area_taglia`, `ex_area_incolla`,
   `ex_area_seleziona_tutto`, in memoria condivisa, quindi si copia in un
   programma e si incolla in un altro. MA vale per il controllo `ex_area` del
   toolkit: i campi di un modulo del BROWSER sono rettangoli disegnati, non
   `ex_area`, e li' non c'e'.

G. I commenti che nominano `/bin/svga` sono da correggere: il programma sta in
   `/dev/svga.drv`. Sono in bin/hwconfig/hwconfig.c, kernel/include/cfg.h e
   bootloader/stage2/loader.asm. Un nome sbagliato in un commento e' il modo
   in cui si conclude che una cosa non esista — e' successo qui.
